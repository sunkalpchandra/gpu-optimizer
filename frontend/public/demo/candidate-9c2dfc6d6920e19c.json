{"candidate_id":"9c2dfc6d6920e19c","task":"reduction","shape":[16777216],"config":{"BLOCK_SIZE":4096,"num_warps":2,"strategy":"atomic","dtype":"float32"},"engine":"simulated","status":"ok","latency_ms":0.06206197085188642,"source":"# Triton kernel: reduce_atomic_kernel\n# Candidate configuration:\n#   BLOCK_SIZE = 4096\n#   dtype = float32\n#   num_warps = 2\n#   strategy = atomic\n\n@triton.jit\ndef reduce_atomic_kernel(x_ptr, out_ptr, n_elements, BLOCK_SIZE: tl.constexpr):\n    \"\"\"Each program atomically adds its slice sum into out_ptr[0].\"\"\"\n    pid = tl.program_id(0)\n    offs = pid * BLOCK_SIZE + tl.arange(0, BLOCK_SIZE)\n    x = tl.load(x_ptr + offs, mask=offs < n_elements, other=0.0).to(tl.float32)\n    tl.atomic_add(out_ptr, tl.sum(x, axis=0))\n"}