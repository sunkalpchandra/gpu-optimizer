{"candidate_id":"2d6ff11fea45631c","task":"reduction","shape":[16777216],"config":{"BLOCK_SIZE":512,"num_warps":2,"strategy":"two_pass","dtype":"float32"},"engine":"simulated","status":"ok","latency_ms":0.055611285056441154,"source":"# Triton kernel: reduce_partial_kernel\n# Candidate configuration:\n#   BLOCK_SIZE = 512\n#   dtype = float32\n#   num_warps = 2\n#   strategy = two_pass\n\n@triton.jit\ndef reduce_partial_kernel(x_ptr, partial_ptr, n_elements, BLOCK_SIZE: tl.constexpr):\n    \"\"\"Each program sums one BLOCK_SIZE slice into partial_ptr[pid].\"\"\"\n    pid = tl.program_id(0)\n    offs = pid * BLOCK_SIZE + tl.arange(0, BLOCK_SIZE)\n    x = tl.load(x_ptr + offs, mask=offs < n_elements, other=0.0).to(tl.float32)\n    tl.store(partial_ptr + pid, tl.sum(x, axis=0))\n"}